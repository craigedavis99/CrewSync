{
  "version": 2,
  "framework": "vite",
  "installCommand": "npm install",
  "buildCommand": "npm run build:client",
  "outputDirectory": "dist/public",
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
