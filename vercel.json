{
  "version": 2,
  "framework": "vite",
  "installCommand": "npm install",
  "buildCommand": "npm run build:client && rm -rf public && mkdir -p public && cp -r dist/public/* public/",
  "outputDirectory": "public",
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
